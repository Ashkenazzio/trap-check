# Core - minimal dependencies for basic pipeline
python-dotenv>=1.0.0
httpx>=0.25.0

# SerpAPI (optional - mock data works without it)
google-search-results>=2.4.2

# UI
gradio>=4.0.0

# Language detection for review analysis
langdetect>=1.0.9

# === RAG Dependencies ===
# Adds ~2GB for PyTorch - required for RAG integration
chromadb>=0.4.0
sentence-transformers>=2.2.0

